<br>
<div class="container">
  <h2><%= @post.title %></h2>
  <% if @post.tags %>
    <h6>Tag(s): <% @post.tags.each do |tag| %>
      <%= tag.tag_name %><h6>
    <% end %>
  <% end %>
  <p> <%= @post.body %> </p>
</div>
